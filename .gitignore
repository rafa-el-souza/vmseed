# Personal config (keep the .example tracked)
/fedora-cloud.conf

# Generated artifacts
/build/

# Downloaded images, checksums and keyring
/images/

# Never commit key material or images (SSH keys live in ~/.ssh, outside the repo)
*.pem
*.qcow2
*.img
